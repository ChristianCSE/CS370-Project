package cluster;

public class JaccardSimilarity
{
	public double findSimilarity(SparseVector a, SparseVector b)
	{
		int length = (a.getIndices().length > b.getIndices().length) ? b.getIndices().length : a.getIndices().length; 
		
		return 0;
	}
}
